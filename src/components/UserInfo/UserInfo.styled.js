import styled from "styled-components";

const StyledUserInfo = styled.p`
    font-size: 0.9rem;
    
    & span {
        display: block;
        font-style: italic;
    }
`

export default StyledUserInfo